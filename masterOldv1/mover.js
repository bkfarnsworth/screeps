module.exports = function (creep) {

// 		if(creep.harvest(sources[0]) == ERR_NOT_IN_RANGE) {
		//	creep.moveTo(new RoomPosition('W11S6'));
// 		}			

// var exit = FIND_EXIT_RIGHT;
// creep.moveTo(creep.pos.findClosest(exit));

}